apiVersion: v1
kind: Service
metadata:
  name: {{ include "argo-learning.fullname" . }}
  labels:
    {{- include "argo-learning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argo-learning.selectorLabels" . | nindent 4 }}
