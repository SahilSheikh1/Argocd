apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argo-learning.fullname" . }}-test-connection"
  labels:
    {{- include "argo-learning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argo-learning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
